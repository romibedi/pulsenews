# ---------------------------------------------------------------------------
# S3 — REMOVED (replaced by App Runner serving static files)
# The React frontend is now bundled into the Docker image and served by Express.
# ---------------------------------------------------------------------------
