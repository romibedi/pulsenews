# ---------------------------------------------------------------------------
# API Gateway — REMOVED (replaced by App Runner)
# All API routes are now served directly by Express in the App Runner container.
# ---------------------------------------------------------------------------
