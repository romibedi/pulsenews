# ---------------------------------------------------------------------------
# EventBridge — REMOVED (replaced by node-cron in App Runner)
# RSS ingestion runs every 15 minutes via node-cron inside the container.
# ---------------------------------------------------------------------------
